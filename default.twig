{% include "header.twig" %}
	<h2>Yes, this is default</h2>

	<h1>Heading 1</h1>
	<h2>Heading 2</h2>
	<h3>Heading 3</h3>
	<h4>Heading 4</h4>
	<h5>Heading 5</h5>
	<h6>Heading 6</h6>
	<p>Paragraph</p>
	<a>Hyperlink</a>
	<small>Small</small>
	<span class="unicorn">Unicorn</span>
	
	<!-- type('classname', 'fieldname', {"label?": "Default Value"})|raw -->
	{{widget('widgetclass', 'companyname', {"business": "Company Name"})|raw}}
	
	{{ page.seoLang }}
	{{ page.keywords }}
	{{ page.description }}
	{{ page.backgroundClass }}
	{{ page.request }}
	{{ page.templateType }}

	{{ page.colorSwatch }}

	<!-- what is this? -->
	{% block content %}
	{% endblock %}
	<!-- eh? -->

{% include "footer.twig" %}